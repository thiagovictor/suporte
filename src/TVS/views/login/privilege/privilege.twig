{% extends "template.twig" %}


{% block superior %}
    <a type="button" href="{{path('privilege_new')}}" class="{{class_btn}}" role="button">
        <span class="{{class_btn_new}}" aria-hidden="true"></span> Novo
    </a>
{% endblock %}

{% block conteudo %}
    <table class="{{class_table}}">
        <thead>
            <tr>
                <th>ID</th>
                <th>Rota</th>
                <th>Visualizar</th>
                <th>Novo</th>
                <th>Editar</th>
                <th>deletar</th>
                <th>Usu&aacute;rio</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for privilege in result %}
                <tr>
                    <td>{{ privilege.id }}</td>
                    <td>{{ privilege.route.route }}</td>
                    <td>{{ privilege.display }}</td>
                    <td>{{ privilege.new }}</td>
                    <td>{{ privilege.edit }}</td>
                    <td>{{ privilege.delete }}</td>
                    <td>{{ privilege.user.username }}</td>  
                    <td>
                        <a href="{{path('privilege_edit',{'id':privilege.id})}}" class="{{class_btn}}" aria-label="Left Align">
                            <span class="{{class_btn_edit}}" aria-hidden="true"></span>
                        </a>
                        <a href="{{path('privilege_delete',{'id':privilege.id})}}" class="{{class_btn}}" aria-label="Left Align">
                            <span class="{{class_btn_delete}}" aria-hidden="true"></span>
                        </a>

                    </td>
                </tr>   
            {% endfor %}
        </tbody>
    </table>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <nav>
                <ul class="pagination">
                    {%for i in 1..numero_paginas%}
                        {%if loop.first %}
                            <li class="prev {%if page_atual==1%} disabled{%endif%}"><a {%if page_atual > 1%} href="{{path('privilege_listar_pagination',{'page':page_atual-1})}}" {%endif%}><<</a></li>
                            {%endif%}
                            {%if numero_paginas > 0 %}
                            <li {%if page_atual==loop.index%} class="active"{%endif%}><a href="{{path('privilege_listar_pagination',{'page':loop.index})}}">{{loop.index}}</a></li> 
                            {% endif%}
                            {%if loop.last %}
                            <li class="next{%if page_atual==numero_paginas%} disabled {%endif%}"><a {%if page_atual < numero_paginas%} href="{{path('privilege_listar_pagination',{'page':page_atual+1})}}" {%endif%}>>></a></li>
                            {%endif%}
                        {%endfor%}
                </ul>
            </nav>
        </div>
        <div class="col-md-4"></div>
    </div>


{% endblock %}
